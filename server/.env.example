PORT=5000
NODE_ENV=development

# MongoDB URI — أنشئ cluster مجاني على mongodb.com
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/pokebattle

# JWT Secret — غيّره لنص عشوائي طويل
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Client URL (للـ CORS)
CLIENT_URL=http://localhost:3000
